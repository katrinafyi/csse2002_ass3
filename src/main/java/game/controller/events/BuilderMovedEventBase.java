package game.controller.events;

import csse2002.block.world.Builder;

public class BuilderMovedEventBase extends BuilderEventBase {
    public BuilderMovedEventBase(Builder builder) {
        super(builder);
    }
}
